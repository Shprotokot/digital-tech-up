# 🛠️ Примечания для разработки

## 📝 Контактная форма

### Режимы работы

#### 🔷 DEV режим (текущий)
Когда вы тестируете сайт локально или в preview:
- ✅ Форма работает в режиме **симуляции**
- ✅ При отправке показывается успешное сообщение
- ✅ Данные **логируются в консоль** браузера (F12 → Console)
- ❌ Реальная отправка email **НЕ происходит**

**Как проверить:**
1. Откройте консоль браузера (F12)
2. Заполните и отправьте форму
3. В консоли увидите: `📧 DEV MODE: Форма отправлена (симуляция)`
4. Там же будут все данные формы

#### 🟢 PROD режим (на хостинге)
После деплоя на хостинг Рег.ру с PHP:
- ✅ Форма отправляет **реальные email** на `noreply@digital-tu.ru`
- ✅ Работает через PHP скрипт `/send-email.php`
- ✅ Есть валидация и защита от спама
- ✅ IP и время отправки сохраняются в письме

---

## 🚀 Подготовка к деплою на Рег.ру

### Шаг 1: Загрузите файлы на хостинг

```bash
# Основные файлы сайта
/index.html
/assets/
/components/
# ... остальные файлы сборки

# PHP скрипт для email (скопировать из корня проекта)
/send-email.php  ← ВАЖНО: должен быть в КОРНЕ сайта, не в /public/!
```

### Шаг 2: Создайте почтовый ящик

В панели управления Рег.ру:
1. Перейдите в раздел "Почта"
2. Создайте ящик `noreply@digital-tu.ru`
3. Проверьте DNS записи (MX)

### Шаг 3: Проверьте работу PHP

Откройте: `https://ваш-домен.ru/send-email.php`

Если видите JSON с ошибками - ✅ PHP работает!
```json
{"success":false,"errors":["Имя обязательно для заполнения",...]}
```

### Шаг 4: Протестируйте форму

1. Заполните форму на сайте
2. Нажмите "Отправить заявку"
3. Проверьте почту `noreply@digital-tu.ru`

---

## 📧 Структура отправки

```
Пользователь заполняет форму
          ↓
JavaScript (ContactForm.tsx)
          ↓
POST → /send-email.php
          ↓
PHP mail() функция
          ↓
Почта: noreply@digital-tu.ru
```

---

## 🔍 Отладка

### В dev режиме (сейчас):
```javascript
// Откройте консоль (F12) и увидите:
📧 DEV MODE: Форма отправлена (симуляция)
📧 На продакшене данные будут отправлены на: noreply@digital-tu.ru
📧 Данные формы: {
  name: "Иван Иванов",
  email: "ivan@example.com",
  phone: "+7 (999) 123-45-67",
  message: "Хочу заказать сайт"
}
```

### На хостинге (prod):
```php
// Проверьте логи PHP на хостинге
// Или создайте тестовый файл test-email.php:
<?php
$test = mail(
  'ваш-email@example.com',
  'Тест',
  'Если видите это письмо - PHP mail() работает!',
  'From: noreply@digital-tu.ru'
);
echo $test ? '✅ Работает' : '❌ Не работает';
?>
```

---

## ⚙️ Настройки

### Изменить получателя писем:

В файле `/public/send-email.php` измените:
```php
$to = 'noreply@digital-tu.ru'; // ← Измените на нужный email
```

### Добавить копию на другой email:

```php
// В файле send-email.php после строки с $headers
$headers .= "Cc: info@digital-tu.ru\r\n";  // Видимая копия
// или
$headers .= "Bcc: admin@digital-tu.ru\r\n"; // Скрытая копия
```

### Изменить тему письма:

```php
$subject = 'Новая заявка с сайта DIGITALTechUp'; // ← Измените тему
```

---

## 📖 Полная документация

См. файл `EMAIL_SETUP.md` для подробных инструкций по настройке на хостинге.

---

## ✅ Чеклист перед деплоем

- [ ] Скопировать `/send-email.php` (из корня проекта) в корень сайта на хостинге
- [ ] Убедиться что файл в корне: `https://домен.ru/send-email.php` (не в /public/)
- [ ] Создать почтовый ящик `noreply@digital-tu.ru` в панели Рег.ру
- [ ] Проверить MX записи для домена `digital-tu.ru`
- [ ] Проверить работу PHP: открыть `домен.ru/send-email.php`
- [ ] Протестировать форму на реальном сайте
- [ ] Проверить получение писем в `noreply@digital-tu.ru`
- [ ] Проверить папку "Спам" если письма не приходят

---

## 💡 Полезные команды

### Проверка прав доступа к файлу (SSH):
```bash
chmod 644 send-email.php
```

### Просмотр логов PHP (если доступны):
```bash
tail -f /path/to/php-error.log
```

---

## 🆘 Если что-то не работает

1. **Письма не приходят** → Проверьте папку "Спам"
2. **Ошибка 404** → Файл send-email.php не в корне сайта
3. **Ошибка 500** → Проблема с PHP кодом, проверьте логи
4. **Форма не отправляется** → Откройте консоль (F12) и Network
5. **PHP mail() не работает** → Обратитесь в поддержку Рег.ру

---

📞 **Контакты поддержки Рег.ру:** https://www.reg.ru/support/
